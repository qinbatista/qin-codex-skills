interface:
  display_name: "Management Skill"
  short_description: "Routing records, profiles, and eight-skill mirror"
  default_prompt: >-
    $management-skill: do not load for ordinary exact-scoped read-only work or benchmark worker arms. Manage model experience, profiles, or the approved mirror only for an explicit management request or admitted node. Active private learning is the broad Obsidian Model Switch.md page; local model_experience.json and central TaskModelExperience are legacy read-only. Ordinary text/code executes its saved quality pair; two Real PASS results downgrade one rung, quality FAIL upgrades one, and Spark is scheduled-source-only. Present the result before verification. Start Ending with the producer receipt so its terminal ledger event records the verdict. Never publish private state.
