interface:
  display_name: "Verify Skill"
  short_description: "Mini gate now, Real proof in Ending Task"
  default_prompt: >-
    $verify-skill: run Mini before result release. Treat mini_real Mini PASS as provisional; Ending Real updates the producer, routing learning, and only related sanitized memory. Record producer pair, not verifier pair; missing Obsidian is a no-op. Real correctness failure reopens, operational failure stays neutral, and optimization needs a different verifier.
