interface:
  display_name: "Verify Skill"
  short_description: "Explicit verification or post-result Ending Real"
  default_prompt: >-
    $verify-skill: every user task launches an independent post-result Ending lifecycle. Score it locally. Verification-needed results use ending_verification_plan.py and one global projectless End Task-{task}-{check} per independent real unit/integration/API/build/render/state check. Select each verifier model from its own 0-100 score; all required checks must PASS. After durable PASS, self-archive; the archive may terminate the turn. FAIL/BLOCKED stays visible. Never summarize prior evidence instead of running the assigned check. FAIL records exact command/output/error, starts a separate projectless repair task, and then a fresh verifier, up to three attempts. BLOCKED never counts as verified. Store local evidence and repair chain; receipt-backed producer PASS/FAIL syncs Obsidian. Never let a verifier repair its own target or use a same-task subagent.
