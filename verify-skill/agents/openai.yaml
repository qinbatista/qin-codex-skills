interface:
  display_name: "Verify Skill"
  short_description: "Verification router for scripts, workflows, artifacts, and UI"
  default_prompt: "Use $verify-skill to define the right checks, run real verification, and report the evidence."
