interface:
  display_name: "Verify Skill"
  short_description: "Real tests, reports, and verification"
  default_prompt: "After $workflow-skill routes the task here, use $verify-skill to run concrete evidence checks, verify real outcomes against the user's requirement, compare regressions when a baseline exists, apply artifact-specific visual standards, choose chat/Markdown/PDF evidence by complexity, try safe repair routes for fixable failures, and report the real input, method, output, and pass or stop reason."
