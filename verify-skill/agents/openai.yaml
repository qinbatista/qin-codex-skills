interface:
  display_name: "Verify Skill"
  short_description: "Explicit verification or post-result Ending Real"
  default_prompt: >-
    $verify-skill: every durable code change emits ending-required;intentionally_skipped_simple_task is only for eligible small non-code work. After CODE READY launch one global-only projectless Ending via ending_verification_plan.py;exact create_thread target={"type":"projectless"};origin project=context only. codex_app__list_threads must read back projectId=null/absent;project/current-task/same-task-subtask placement is BLOCKED;return without polling. Sole lifecycle controller=gpt-5.3-codex-spark|xhigh;unavailable=>registry-floor gpt-5.6-luna|low. Deterministic checks run directly. Semantic runtime/integration/code-quality/prompt/UI/visual checks may use Terra/Sol ENDING_CHECK_WORKER nodes;read listed Skills;run one check;write fresh evidence;never edit/repair/route/lifecycle. Capture immutable origin;all checks PASS;task stays visible. FAIL exact evidence via codex_app__send_message_to_thread;origin repairs;fresh Spark-first Ending,up to three. Never self-repair or substitute a same-task subagent for Ending.
