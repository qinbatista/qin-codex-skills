interface:
  display_name: "Verify Skill"
  short_description: "Explicit verification or post-result Ending Real"
  default_prompt: >-
    $verify-skill: every new task launches one independent projectless Ending after result through ending_verification_plan.py; reads and one-value edits use the smallest completion/record check. One plan may retain distinct checks, but one Ending runs them and closes memory/classification once. Ending primary is gpt-5.3-codex-spark|xhigh; score scopes checks only. Only explicit model, effort, scheduler, or required-modality unavailability permits registry-floor gpt-5.6-luna|low; correctness, protocol, execution, timeout, or acceptance failure never switches verifier. Capture immutable origin/session, host, project ID/root; real checks all PASS. Keep terminal task visible. FAIL sends exact evidence via codex_app__send_message_to_thread; origin repairs and starts fresh Spark-first Ending, up to three. Never self-repair, auto-archive, expose reasoning, or use same-task subagent.
