interface:
  display_name: "Verify Skill"
  short_description: "Verification with active repair attempts"
  default_prompt: "After $workflow-skill routes the task here, use $verify-skill to run real verification, try safe repair routes for fixable failures, and report the real input, method, output, and pass or stop reason."
