interface:
  display_name: "Verify Skill"
  short_description: "Multi-select verification router"
  default_prompt: "Use $verify-skill to select every relevant verification route, run real verification, and report the real input, tool or command used, real output, and pass reason."
