# Retired Plain C# Profile

`execution_domain=csharp` is history-only. New `csharp`, `c#`, and Unity C# work resolves to the single active `execution_domain=unity_csharp` profile and reads [`unity-csharp-rules.md`](unity-csharp-rules.md).

Do not load this page as a second C# rule set and do not create new plain-C# routing evidence. It remains only so historical receipts and links have an explicit migration explanation.
