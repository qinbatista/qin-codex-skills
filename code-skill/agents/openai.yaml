interface:
  display_name: "Code Skill"
  short_description: "Universal code-writing process and domain executor"
  default_prompt: >-
    $code-skill: artifact-free exact read-only lookup/audit stays skill-free; all code writing loads references/code-writing-philosophy.md. Unity game C# reads nearest AGENTS.md plus references/unity-game-code-structure-design.md; local cannot weaken Controller/Manager/ScriptableObject core. Requested Cache/report output requires the runner. Score every submission 0-100; safe small code tries Spark-low; quality failure suppresses the matching band. Capture immutable origin session; exact project binding stays context. ending_verification_plan.py creates one projectless End Task with smallest completion/record check or explicit checks; all must PASS and remain visible. Ending=gpt-5.3-codex-spark|xhigh; unavailable=>gpt-5.6-luna|low. Never auto-archive/delete. Record difficulty/pairs plus attempts, first/retry pass, suitability, and Obsidian link. codex_app__send_message_to_thread repairs in the source session; fresh projectless verification starts Spark-first. Never self-verify or use a same-task subagent.
