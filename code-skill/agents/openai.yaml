interface:
  display_name: "Code Skill"
  short_description: "Universal code-writing process and domain executor"
  default_prompt: >-
    $code-skill: artifact-free exact read-only lookup/audit stays skill-free;references/code-writing-philosophy.md;relevant language/platform/domain Skills. Unity game C#:references/unity-game-code-structure-design.md;Controller/Manager/ScriptableObject core;every self-contained Unity C# statement/expression that fits clearly on one physical line;do not vertically wrap code that fits or split a ternary assignment only to indent its branches unless required or requested. exactly one smallest local Quick Check;publish CODE READY;broad tests/builds/UI/full lint/log cleanup/repeated review move to Ending. Code normally exposes real_test through Quick Check and emits ending-required;ending_verification_plan.py→one visible global-only projectless End Task;list_threads=>projectId=null/absent;gpt-5.3-codex-spark|xhigh;gpt-5.6-luna|low;ENDING_CHECK_WORKER read listed Skills;never edit/repair/route/lifecycle;All checks PASS;codex_app__send_message_to_thread→immutable origin→fresh Spark-first Ending. Never self-verify.
