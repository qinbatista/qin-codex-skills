interface:
  display_name: "Code Skill"
  short_description: "Code mutation or execution; exact read-only work stays skill-free"
  default_prompt: >-
    $code-skill: do not load for any exact-scoped read-only lookup, audit, transform, or workflow reconstruction, even over Python/C#/Unity sources. Use directly for implementation, edit, execution, debug, refactor, authored tests/probes, or domain reasoning beyond the supplied scope, or execute a positively admitted node. Inline work uses matching language rules, completes and presents the result immediately with no foreground Mini/Fast Verify, then hands proportional Real Verify to Ending Task. Ending time is excluded from first-result time. Never reselect an admitted pair.
