interface:
  display_name: "Optimization Skill"
  short_description: "Reusable improvements with independent Ending proof"
  default_prompt: >-
    $optimization-skill: do not load from benchmark repetition alone or exact-scoped read-only work. Use only for requested optimization, an authorized reusable improvement, or an admitted node. Preserve behavior, finish code, run one smallest Quick Check, and present CODE READY before verification. Code optimization normally exposes real_test through Quick Check; no surface records intentionally_skipped_simple_task with ending_skip_reason=no_real_test_or_information_or_memory_update. Parent starts one visible global-only projectless Ending with exact create_thread target {"type":"projectless"}; list_threads must read back projectId=null/absent, and project/current-task/subtask placement is BLOCKED. Its sole controller is gpt-5.3-codex-spark|xhigh; Luna-low is controller-unavailability-only. Saved semantic checks may use Terra/Sol ENDING_CHECK_WORKER nodes that never edit or own lifecycle. A different worker verifies; Ending time stays separate from first-result time. Never self-certify savings.
