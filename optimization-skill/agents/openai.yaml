interface:
  display_name: "Optimization Skill"
  short_description: "Post-task workflow and skill optimization"
  default_prompt: "After $workflow-skill routes the task here, use $optimization-skill to optimize repeated fixed workflows in the owning user skill after the task is complete, using other skills as needed and verifying the faster path with real execution."
