interface:
  display_name: "Optimization Skill"
  short_description: "Reusable improvements with independent Ending proof"
  default_prompt: >-
    $optimization-skill: do not load from benchmark repetition alone or exact-scoped read-only work;requested optimization/authorized reusable improvement/admitted node only. Preserve behavior;one Quick Check;CODE READY. Non-trivial code/process optimization is material=>global projectless Ending+durable memory;only explicit trivial_value_only may skip. Spark-xhigh first;quota/5-hour/provider cooldown;skip cooling model for next stronger controller. FAIL=>fresh independent projectless Repair;never contact/steer/interrupt/terminate/handoff/move/mutate existing sessions;active write conflict waits;fresh parent-linked Ending. Workers evidence-only;all PASS;never self-certify savings.
