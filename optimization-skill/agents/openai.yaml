interface:
  display_name: "Optimization Skill"
  short_description: "Reusable improvements with independent Ending proof"
  default_prompt: >-
    $optimization-skill: do not load from benchmark repetition alone or for exact-scoped read-only work. Use directly only for user-requested optimization or an explicitly authorized reusable improvement, or execute a positively admitted node. Eligible production uses the catalog-generated ladder and matching Obsidian context. Preserve behavior, present the optimized result immediately with no foreground Mini/Fast Verify, then start a visible projectless Ending for every new optimization task with the producer receipt and hand before/after evidence to a different Real verifier; trivial results use a smallest completion/record check. Ending time is excluded from first-result time; never self-certify savings.
