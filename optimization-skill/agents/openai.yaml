interface:
  display_name: "Optimization Skill"
  short_description: "Multi-route workflow optimization"
  default_prompt: "Use $optimization-skill to select every relevant optimization route, prepare the required references, convert deterministic parts into local scripts or resources when useful, follow code-skill for any code, and verify with real execution."
