interface:
  display_name: "Test Skill"
  short_description: "Test executor routed by workflow-skill"
  default_prompt: "After $workflow-skill routes the task here, use $test-skill to select every relevant evidence route, run real usage tests, and generate a concise PDF report that shows the real input, tool or command used, real output, and why the observed output passes."
