interface:
  display_name: "Task Analyze"
  short_description: "Routing, benchmarks, maintenance, and admitted graph strategy"
  default_prompt: >-
    score 0-100;resolve entry moves up/down;show band/route;2+ result stages=>graph;Model stages lists every pair/status;history picks lowest-correct;obsidian_adaptive_model_runner.py;LOCKED_ROUTE_NODE emits code-rule-notice before source work;Code Gate loads universal philosophy+one active language profile+matched categories;missing notice/reference fails;C# aliases use only Unity C#;one smallest Quick Check;publishes CODE READY;Ending surfaces=real_test|information_update|memory_update;ending_skip_reason=no_real_test_or_information_or_memory_update;global projectless End via;ending_verification_plan.py;exact create_thread target;codex_app__list_threads;projectId=null/absent;same-task-subtask placement is BLOCKED is not verified;returns without polling;gpt-5.3-codex-spark|xhigh;registry-floor gpt-5.6-luna|low;ENDING_CHECK_WORKER;read listed Skills;never edit/repair/route/lifecycle;Bind immutable origin;trigger=100%;all checks PASS;visible;repair_prompt;codex_app__send_message_to_thread;fresh Spark-first End<=3
