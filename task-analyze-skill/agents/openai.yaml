interface:
  display_name: "Task Analyze"
  short_description: "Routing, benchmarks, maintenance, and admitted graph strategy"
  default_prompt: >-
    score 0-100;resolve entry;show band/route;2+ result stages=>graph;Model stages lists every pair/status;history picks lowest-correct;entry moves up/down;obsidian_adaptive_model_runner.py. LOCKED_ROUTE_NODE loads required Skills;one smallest Quick Check;publishes CODE READY;stops. Ending surfaces=real_test|information_update|memory_update;no surface=>skip+ending_skip_reason=no_real_test_or_information_or_memory_update. global projectless End via ending_verification_plan.py;exact create_thread target {"type":"projectless"}. codex_app__list_threads requires projectId=null/absent before ack;project/current-task/same-task-subtask placement is BLOCKED;returns without polling. Controller=gpt-5.3-codex-spark|xhigh;unavailable=>registry-floor gpt-5.6-luna|low. ENDING_CHECK_WORKER nodes read listed Skills;never edit/repair/route/lifecycle. Bind immutable origin;trigger=100%;all checks PASS;visible FAIL:repair_prompt via codex_app__send_message_to_thread;origin repairs;fresh Spark-first End<=3;BLOCKED is not verified
