interface:
  display_name: "Task Analyze"
  short_description: "Routing, benchmarks, maintenance, and admitted graph strategy"
  default_prompt: >-
    score 0-100;resolve entry;show band/route;2+ stages=>per-stage graph;final Model stages has every difficulty/pair/status;History picks lowest-correct;entry moves up/down;no-history<=entry;obsidian_adaptive_model_runner.py;LOCKED_ROUTE_NODE produces result/Quick Check only;parent captures immutable origin session;Small low-risk edit<=24 tries Spark-low;quality FAIL suppresses band;Every new user task requires a projectless global End Task, including read/open/one-value results with a smallest completion/record check;ending_verification_plan.py;bind exact project and origin session;projectless End/check;ack(threadId+hostId+projectId);audit=100%;all PASS,visible;never auto-archive/delete;Assessment:stage difficulty/pairs,attempt,first/retry,suitability;Obsidian link;No receipt still records non-learning observation;FAIL/mismatch repair_prompt via codex_app__send_message_to_thread origin;fresh projectless End<=3;BLOCKED is not verified;Never emulate;same-task/self-verify forbidden
