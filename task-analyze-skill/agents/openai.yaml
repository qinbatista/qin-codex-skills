interface:
  display_name: "Task Analyze"
  short_description: "Routing, benchmarks, maintenance, and admitted graph strategy"
  default_prompt: >-
    score 0-100;resolve entry;show band/route;2+ result stages=>graph;Model stages lists every pair/status;history picks lowest-correct;entry moves up/down;obsidian_adaptive_model_runner.py;LOCKED_ROUTE_NODE=result/Quick Check only;small low-risk edit<=24 tries Spark-low and records intentionally_skipped_simple_task. standard+ or medium/high-risk/multi-stage=>ending-required: parent creates projectless global End via ending_verification_plan.py and ack;missing ack=>BLOCKED. Ending primary=gpt-5.3-codex-spark|xhigh;score scopes check only;explicit model/effort/scheduler/modality unavailable=>only registry-floor gpt-5.6-luna|low;quality/protocol/execution/timeout/mismatch never switches verifier. Bind immutable origin/project;ack actual pair;trigger=100%;all checks PASS;visible. FAIL sends repair_prompt via codex_app__send_message_to_thread;origin repairs;fresh Spark-first End<=3;BLOCKED is not verified;no emulate/same-task/self-verify.
