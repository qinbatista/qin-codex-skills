interface:
  display_name: "Task Analyze"
  short_description: "Routing, benchmarks, maintenance, and admitted graph strategy"
  default_prompt: >-
    score 0-100;resolve entry up/down;show band/route;classify stages;2+=>graph;route-required=no model;full Task Analyze+run-plan once;Model stages:each pair/status;history=lowest-correct;obsidian_adaptive_model_runner.py;LOCKED_ROUTE_NODE emits code-rule-notice before source;Code Gate=universal philosophy+one active language profile+matched categories;missing notice/reference fails;C# aliases only Unity C#;one Quick Check;CODE READY;Ending surfaces=real_test|information_update|memory_update;none=>ending_skip_reason=no_real_test_or_information_or_memory_update;global projectless End via ending_verification_plan.py;exact create_thread target;codex_app__list_threads;projectId=null/absent;same-task-subtask BLOCKED/not verified;return without polling;gpt-5.3-codex-spark|xhigh;fallback gpt-5.6-luna|low;ENDING_CHECK_WORKER reads Skills;never edits/repairs/routes/lifecycle;immutable origin;trigger=100%;all PASS;visible;repair_prompt+codex_app__send_message_to_thread;fresh Spark-first End<=3
