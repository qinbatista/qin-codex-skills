interface:
  display_name: "Task Analyze"
  short_description: "Explicit routing, benchmarking, maintenance, or admitted graph strategy"
  default_prompt: >-
    $task-analyze-skill: load this full skill only for explicit routing/model strategy, admitted different-pair delegation/graphs, or routing maintenance/benchmarks. Ordinary work stays inline. Exact-scoped read-only work stays on the current model with no subagent/route. An exact named-source audit first runs one bounded rg per authoritative file for every exact user-named target and direct definition, anchors named members directly, never adds enclosing-class/call-site anchors or guesses identifier families, answers once, and presents now with no plan/skill load/broad search/reread/full-file/pre-result check. Ending Real Verify follows and reopens failures. Other ordinary work also avoids full-skill load, entry resolution, memory search, Workflow, child receipts, and foreground verification. Different-pair delegation needs frozen Real-pass trial=false end-to-end evidence beating Direct in tokens and first-result time. Keep hookless and hide plans.
