# Task Lifecycle

Merge this section into `~/.codex/AGENTS.md`.

Eligible text/code MUST run adaptive producer. Saved highest-family ladder; only explicit user model-update refreshes it from local `models_cache.json`. never fetch; absent cache keeps it. Obsidian broad `Model Switch.md` pages retain project/task/module/file/symbol as fields only; no hierarchy notes or local JSON authority. Priority: easy=low, complex=high; zero-result uses contextual quality pair; publish then return. Prompt/AI-instruction work loads `prompt-skill`. Durable edits: `project-memory-skill`; recall, then record reason/result/verification/files after Ending. Every non-ENDING_TASK_WORKER task launches independent Ending; routed Ending uses `--producer-receipt` and writes PASS/FAIL to a broad page. Failure logs fields before repair; re-present with another verifier. Parallel docs/logs. Final requires PASS or BLOCKED. No hook. Exact read-only: one bounded rg/file at exact members/aliases; no subagent/route/plan, guesses, broad/reread/full read/pre-result check.
