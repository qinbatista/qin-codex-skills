Merge this section into `~/.codex/AGENTS.md` and `~/AGENTS.md`.

# Task Lifecycle

Route:model by difficulty/info;effort by steps;same-class fail→stronger;new topic resets;notify user early;entry≠solver.

Code|standard+|medium/high-risk|multi-stage=>Ending-required.Required Skills→code→Quick Check→`CODE READY`;broad verification→Ending.No raw prompts/results/reasoning,paths,secrets,emails,IDs.Coverage:project/module;code symbol;module=`__module__`.

Score every submission 0-100:small 0-24,standard 25-49,complex 50-74,advanced 75-100;show compact score/model/route status.Resolve entry first;exact step-capability/band history wins:Sol/high may downgrade,Luna-max/lower may upgrade;no match stays <=entry;reuse recovered/lowest-correct.native project→Model Switch→category→shared-category wikilinks;no JSON sidecar;2+ material result stages auto-activate one graph;each result/Ending records score,band,pair,purpose,deps,stop.Eligible low-risk low-ambiguity text/code/write/execute score<=24 executes Spark-low first;Ending quality FAIL suppresses that fingerprint.Dependency-ready independent nodes run parallel;shared writes/order/output deps stay linear.Before eligible:resolve CODEX_HOME;pipe exact task once via platform Python to `skills/task-analyze-skill/scripts/obsidian_adaptive_model_runner.py`;never read/edit/answer inline.LOCKED_ROUTE_NODE loads required Skills,does code/result+one Quick Check,emits CODE READY,stops;parent launches one End after every `ending-required` receipt.ENDING_TASK_WORKER is the sole lifecycle controller:gpt-5.3-codex-spark|xhigh;score scopes checks only;unavailable=>registry-floor Luna-low;direct checks;saved checks use `ENDING_CHECK_WORKER`;no nested End/Fix.ENDING_CHECK_WORKER reads listed Skills,runs one saved check,writes evidence,no edits/routing/lifecycle/repair.Multi-node saves schema-2 dynamic_task_graph;calls task_route_dispatcher.py run-plan;no benchmark.Exact one-source/tool/image read-only uses `task_complexity_score.py`;no reread/full-read/precheck;output artifact MUST runner.2 Real PASS trial down 1 rung;quality FAIL up 1;missing Obsidian queues projection;local history continues.Producer owns files/skills/one Quick Check only.`ending-required` or `ending_required=true`=>one independent global projectless Ending:capture immutable origin session;resolve root with codex_app__list_projects;run ending_verification_plan.py create-launches --project-id(context-only);call exact codex_app__create_thread target `{"type":"projectless"}` with no projectId/environment/current-thread fields;read back exact thread with codex_app__list_threads and require projectId=null/absent;ack threadId+hostId+originProjectId+threadProjectId=null+actual pair;audit-launches end_task_trigger_rate=100%;project/current-task/same-task-subtask placement or missing acknowledgement=>BLOCKED;all checks PASS.`intentionally_skipped_simple_task`=>no End Task and only for eligible non-code.End Task stays globally visible;never auto-archive/delete.Ending:score+pair,attempts,first/retry pass,suitability,next pair/action,Obsidian link/status;terminal memory/classification/record closeout.FAIL/mismatch:repair_prompt→codex_app__send_message_to_thread(origin);origin repairs→Quick Check→receipt→fresh global projectless End Task --repair-of-lifecycle-id;up to 3 repairs;missing source session or failed prompt submission is BLOCKED;BLOCKED only unavailable/external/limit;never emulate another Ending,repair in verifier,or wait for origin;bounded check-worker waits occur only inside detached Ending.Terminal events write local+same-ID Obsidian records;only producer receipts move routing;future routes merge/dedupe pairs,fingerprint,score/band,direction,recovery.Final Ending PASS/BLOCKED

`model_identity_disclosure.py render` outputs verbatim `Complexity:`+`· Model:`+`· Route:` then `Evidence:`=`runtime receipt|verified entry (no runtime receipt)|task assignment (no runtime receipt)|configured selection (no runtime receipt)|unavailable`;changed routes add `Model path:`.For 2+ model stages,`render --manifest` MUST append every score/band/pair/purpose/deps/status in `Model stages (N):`;never show only management.Receipt wins;else known pair;only unavailable=`unknown|unknown`;full routing data stays in receipts/ledgers.

AUTO_BENCHMARK_ENTRY:Direct `gpt-5.6-sol|ultra`;Direct Sol-ultra no Skill;Auto `gpt-5.6-luna|max`;Auto Luna-max stable child/graph;MUST child/graph then return child JSON;gate owns separate Ending;excludes only controller;compare task vs +Ending;all exact PASS;all result/Ending PASS;Primary=time+tokens;totals lower;route/calibration/Ending separate;first-result diagnostic;Sol probe exact result+route;

Unity:report project-relative path;read ProjectVersion.txt;list Editors;resolve `unity editors path <version>`;confirm binary;CLI.External Editor path from valid `Cache/cache_path.json` or discovery+register.Unavailable/unsupported/fail:report+fallback.Never change/install/substitute without approval.

Never control Obsidian UI.Validate `Cache/cache_path.json`;then `CODEX_OBSIDIAN_VAULT`;then `obsidian.json` open-vault;then one bounded search.Cache result;read MyAILLM `AGENTS.md`+project `Knowledge.md`.Unresolved:stop before work,report keys/search.Cold start never waives context.

Host code loads `cross-platform-execution`:portable Python for Windows/macOS/Linux;no `.cmd`/`.sh` selectors.Docker/Unity keep runtime.Skill code runs `prompt-skill` platform checks.

Global Skill change:capability catalog mandatory;full gate PASS before deploy/commit/push;missing/stale/fail blocks;only user retires.Process Skill/AGENTS,real execution,effective project-result memory agree.Ending appends only memory-only correction.Skill/execution defects return origin.Next tasks read effective project-result memory only.

Website:create/change/preview/host/publish=>`sites:sites-building`;publish/.openai/hosting.json=>`sites:sites-hosting`;no substitute;research/navigation excluded.

## Project Cache artifact policy

Before project-support writes,resolve `<project-root>`;test scripts and intermediate code go in `<project-root>/Cache/`:`Cache/tests/<task>`,`Cache/debug/<task>`,or `Cache/images/<task>`.Never use `~/.codex/cache`,`~/.codex/tmp`,or root `tmp/`,`tests/`,`work/`;OS temp excluded.

All local-machine paths,not only Cache paths,are project-root-relative or runtime-derived;never hard-code POSIX home/Windows drive;use native path APIs.

Unavoidable external paths may be in project-external `Cache/cache_path.json`;untracked AI-only state;validate by bounded platform discovery,preserve unrelated keys,never store credentials/publish.Project code never reads it.

project-root `AGENTS.md` is a compact structural contract,not a project notebook:ownership boundaries,critical entry points,hard constraints,owner/source,important Cache.Never remove without explicit authorization.Do not write implementation details,task history,test results,generated data,or troubleshooting;use source/docs/README.Update only for structural changes.
