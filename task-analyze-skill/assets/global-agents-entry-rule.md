Merge this section into `~/.codex/AGENTS.md` and `~/AGENTS.md`.

# Task Lifecycle

Route by task;notify user early.

`real_test`|`information_update`|`memory_update`=>detached projectless Ending;code Quick Check=>`real_test`;none=>`ending_skip_reason=no_real_test_or_information_or_memory_update`;`intentionally_skipped_simple_task`=>no End Task.Skills→code→Quick Check→`CODE READY`;broad verification→Ending.No raw prompts/results/reasoning,paths,secrets,emails,IDs.

Score every submission 0-100:small 0-24,standard 25-49,complex 50-74,advanced 75-100;show score/model/route.Resolve entry first;exact step-capability/band history wins:Sol/high may downgrade,Luna-max/lower may upgrade;no match stays <=entry;reuse lowest-correct.project→Model Switch→category→shared links;classify material stages before any producer:2+ result stages activate one graph;single-stage only=>runner;each stage records score,band,pair,purpose,deps,stop.Eligible low-risk low-ambiguity text/code/write/execute score<=24;same-session gate→Spark-low if no stronger route;Ending FAIL suppresses fingerprint.Execution lifecycle:ultra-simple single result=>direct;other single result=>plan+single execution;multi-result=>plan+dependency graph;emit execution-lifecycle-notice.Model/effort=difficulty+information/steps;quality failure=>strengthen;operational failure is quality-neutral;new topic resets.Independent nodes parallel;shared writes/order/output deps linear.resolve CODEX_HOME;pipe exact task once via platform Python to `skills/task-analyze-skill/scripts/obsidian_adaptive_model_runner.py`;never read/edit/answer inline.`graph-route-required`/`route-required`=>no model ran;full Task Analyze builds one `dynamic_task_graph`;call `task_route_dispatcher.py run-plan` once;no single-producer retry.

LOCKED_ROUTE_NODE emits Code Gate(universal philosophy+one active language profile+matched categories) before source;loads Skills,does code/result+one Quick Check,emits CODE READY,stops;missing Code Gate/reference is routing failure;C# aliases use only Unity C#.Parent launches one End only after final aggregate `ending-required` receipt/release;child/subprocess receipts never launch it.ENDING_TASK_WORKER sole lifecycle controller:gpt-5.3-codex-spark|xhigh;score scopes checks only;unavailable=>Luna-low;no nested End/Fix;same End Task continues one bounded action at a time.ENDING_CHECK_WORKER reads listed Skills,runs one check,writes evidence only;no benchmark.

Exact one-source/tool/image read-only uses `task_complexity_score.py`;no reread/full-read/precheck;output artifact MUST runner.2 Real PASS trial down 1 rung;quality FAIL up 1;missing Obsidian queues projection;local history continues.Producer owns files/skills/one Quick Check only.Only the final aggregate `ending-required` or `ending_required=true` receipt after all result nodes settle=>one independent global projectless Ending when real_test/information_update/memory_update exists;resolve root with codex_app__list_projects;ending_verification_plan.py create-launches --producer-receipt(final) --project-id(context-only);call codex_app__create_thread target;codex_app__list_threads;projectId=null/absent;ack threadId+hostId+originProjectId+threadProjectId=null+actual pair;audit-launches end_task_trigger_rate=100%;project/current-task/same-task-subtask placement;missing acknowledgement=>BLOCKED;all checks PASS.

End Task stays globally visible;never auto-archive/delete;terminal memory/classification/record closeout.FAIL/mismatch:attempts,first/retry pass,suitability,next pair/action,Obsidian link/status;immutable origin session;codex_app__send_message_to_thread;repair_prompt;fresh global projectless End Task;--repair-of-lifecycle-id;up to 3 repairs;missing source session or failed prompt submission is BLOCKED;BLOCKED only unavailable/external/limit;never emulate another Ending,repair in verifier,or wait for origin;bounded check-worker waits occur only inside detached Ending.Terminal events write local+same-ID Obsidian records;only producer receipts move routing.

Host:noninteractive current exec_command/Python;approval_policy="never";never open Terminal.app, Computer Use, or a second terminal to click Allow;host Allow prompt is BLOCKED;stop,no click/emulation.

future routes merge/dedupe pairs,fingerprint,score/band,direction,recovery.AUTO_BENCHMARK_ENTRY:Direct `gpt-5.6-sol|ultra`;Direct Sol-ultra no Skill;Auto `gpt-5.6-luna|max`;Auto Luna-max stable child/graph;MUST child/graph then return child JSON;gate owns separate Ending;excludes only controller;compare task vs +Ending;all exact PASS;all result/Ending PASS;Primary=time+tokens;totals lower;route/calibration/Ending separate;first-result diagnostic;Sol probe exact result+route.Final Ending PASS/BLOCKED.

`model_identity_disclosure.py render`:Complexity: · Model: · Route: · Evidence: runtime receipt|verified entry (no runtime receipt)|task assignment (no runtime receipt)|configured selection (no runtime receipt);changed route=>Model path:;`render --manifest`=>score/band/pair/purpose/deps/status in Model stages;receipt wins;unavailable=unknown|unknown;full routing data.Process Skill/AGENTS,real execution,effective project-result memory agree;Ending appends only memory-only correction;Skill/execution defects return origin;Next tasks read effective project-result memory only.

Unity:relative;ProjectVersion.txt>Editors>`unity editors path <version>`>binary;External Editor=`Cache/cache_path.json`|discover+register;fail=>report+fallback;no install/change/substitute without approval.Host code:`cross-platform-execution` portable Python Windows/macOS/Linux;no .cmd/.sh;Docker/Unity runtime;Skill code:`prompt-skill` platform checks.

No Obsidian UI.Vault:`Cache/cache_path.json`>`CODEX_OBSIDIAN_VAULT`>`obsidian.json`>bounded search;cache.Read MyAILLM `AGENTS.md`+project `Knowledge.md`;unresolved=>stop/report;cold start no waiver.

Global Skill install/update:install first;Codex tests deployed/source/platform,repairs+reinstalls;no user gate work;PASS=>complete;external block=>restore.GitHub commit/push need gate;only user retires.Website=>`sites:sites-building`;publish=>`sites:sites-hosting`;no substitute.

## Project Cache artifact policy

project-support write:<project-root>/Cache/;test scripts/intermediate code;ordinary task artifacts default `tmp-<folder-name>`|`<YYYYMMDD>`;`remote-<name>`:user explicitly asks to retain/save|project contract explicitly marks it as retained;`remote-test/` retained;ordinary test scratch;top-level `tests/` forbidden;legacy top-level directory;forbid `~/.codex/cache`,`~/.codex/tmp`,`tmp/`,`tests/`,`work/`;local-machine path not only Cache paths;project-root-relative/runtime native path APIs;Cache/cache_path.json:bounded AI-only project-external;important Cache:owner/source explicit authorization;project-root `AGENTS.md`:compact structural contract, not a project notebook;ownership boundaries/critical entry points/hard constraints;Do not write implementation details;task history/test results/generated data/troubleshooting=>Obsidian
