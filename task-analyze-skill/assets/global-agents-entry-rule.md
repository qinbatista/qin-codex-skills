This template is written only by the explicit `install-global-agents` command; deploy, pull, and sync preserve user AGENTS.md files.

# Task Lifecycle

notify user early.

`real_test`|`information_update`|`memory_update`|material structural/non-trivial-code/conceptual/process update=>detached projectless Ending;material=>durable project-memory closeout;only explicit `trivial_value_only` with no other surface=>`ending_skip_reason=no_real_test_or_information_or_memory_or_material_update`;`intentionally_skipped_simple_task`=>no End Task.Skills→code→Quick Check→`CODE READY`;broad verification→Ending.No raw prompts/results/reasoning,paths,secrets,emails,IDs.

Score every submission 0-100:small 0-24,standard 25-49,complex 50-74,advanced 75-100;show score/model/route.Resolve entry first;exact step-capability/band history wins:Sol/high may downgrade,Luna-max/lower may upgrade;no match stays <=entry;reuse lowest-correct.project→Model Switch→category→shared links;classify material stages before any producer:2+ result stages activate one graph;single-stage only=>runner;each stage records score,band,pair,purpose,deps,stop.Eligible low-risk low-ambiguity text/code/write/execute score<=24;same-session gate→Spark-low if no stronger route;Ending FAIL suppresses fingerprint.Execution lifecycle:ultra-simple single result=>direct;other single result=>plan+single execution;multi-result=>plan+dependency graph;emit execution-lifecycle-notice;operational failure is quality-neutral;new topic resets.Independent nodes parallel;shared writes/order/output deps linear.resolve CODEX_HOME;pipe exact task once via platform Python to `skills/task-analyze-skill/scripts/obsidian_adaptive_model_runner.py`;never read/edit/answer inline.`graph-route-required`/`route-required`=>no model ran;full Task Analyze=>one `dynamic_task_graph`;call `task_route_dispatcher.py run-plan` once;no single-producer retry.

Complex/advanced/planned_graph=>assess 2+ safe disjoint child sessions or dispatcher;root controls+aggregates;child READY!=done/Ending.

LOCKED_ROUTE_NODE emits Code Gate(universal philosophy+one active language profile+matched categories) before source;loads Skills,does code/result+one Quick Check,emits CODE READY,stops;missing Code Gate/reference is routing failure;C# aliases use only Unity C#.Parent launches one End only after final aggregate `ending-required` receipt/release;child/subprocess receipts never launch it.ENDING_TASK_WORKER controller is Spark-xhigh first:gpt-5.3-codex-spark|xhigh;quota/5-hour/provider-rate/trusted retry-after=>record restricted_at+retry_at/cooldown_until;skip cooling model,next stronger supported controller;expiry=>Spark first;never loop cooling model;quality FAIL creates no cooldown;score scopes checks only;same End Task continues one bounded action at a time.ENDING_CHECK_WORKER reads listed Skills,runs one check,writes evidence only;no benchmark.

Exact one-source/tool/image read-only uses `task_complexity_score.py`;no reread/full-read/precheck;output artifact MUST runner.2 Real PASS trial down 1 rung;quality FAIL up 1;missing Obsidian queues projection;local history continues.Producer owns files/skills/one Quick Check only.Only the final aggregate `ending-required` or `ending_required=true` receipt after all result nodes settle=>one independent global projectless Ending;resolve root with codex_app__list_projects;ending_verification_plan.py create-launches --producer-receipt(final) --project-id(context-only);call codex_app__create_thread target;codex_app__list_threads;projectId=null/absent;ack threadId+hostId+originProjectId+threadProjectId=null+actual pair;audit-launches end_task_trigger_rate=100%;project/current-task/same-task-subtask placement;missing acknowledgement=>BLOCKED;all checks PASS.

End Task stays globally visible;never auto-archive/delete;terminal memory/classification/record closeout.FAIL/mismatch:attempts,first/retry pass,suitability,next pair/action,Obsidian link/status=>fresh independent global projectless Repair Task;immutable origin is context only;never send/steer/interrupt/terminate/handoff/move/mutate any existing task/session;active task owns required write surface=>waits without messaging/interruption;repair=>fresh global projectless End Task with --repair-of-lifecycle-id;up to 3 repairs;BLOCKED only unavailable/external/limit,not active-task waiting;never repair in verifier;bounded check-worker waits occur only inside detached Ending.Terminal events write local+same-ID Obsidian records;only producer receipts move routing.

Host:exec_command/Python;approval_policy="never";never open Terminal.app, Computer Use, or a second terminal to click Allow;host Allow prompt is BLOCKED.

Resources:load `workflow-skill/references/task-resource-lifecycle.md`;exact task-owned path/runtime/UI;durable+last-consumer readback=>LIFO release;delete `Cache/tmp-*` only;retain/defer preexisting/shared/conflicted/Unity/date/remote;cleanup never controls Codex tasks/threads/sessions/Endings.

future routes merge/dedupe pairs,fingerprint,score/band,direction,recovery.AUTO_BENCHMARK_ENTRY:Direct `gpt-5.6-sol|ultra`;Direct Sol-ultra no Skill;Auto `gpt-5.6-luna|max`;Auto Luna-max stable child/graph;MUST child/graph then return child JSON;gate owns separate Ending;excludes only controller;compare task vs +Ending;all exact PASS;all result/Ending PASS;Primary=time+tokens;totals lower;route/calibration/Ending separate;first-result diagnostic;Sol probe exact result+route.Final Ending PASS/BLOCKED.

`model_identity_disclosure.py render`:Complexity: · Model: · Route: · Evidence: runtime receipt|verified entry (no runtime receipt)|task assignment (no runtime receipt)|configured selection (no runtime receipt);changed route=>Model path:;`render --manifest`=>score/band/pair/purpose/deps/status in Model stages;unavailable=unknown|unknown;full routing data.Process Skill/AGENTS,real execution,effective project-result memory agree;Ending appends only memory-only correction;Skill/execution defects launch isolated Repair Task;Next tasks read effective project-result memory only.

Unity:ProjectVersion.txt>Editors>binary;Editor=registry|discover;no install/change without approval.Host code:portable Python Win/macOS/Linux;no .cmd/.sh;prompt-skill platform checks.

No Obsidian UI;Vault:registry>env>config>search;MyAILLM:AGENTS.md+Knowledge.md;unresolved=>stop;cold-start no waiver.

Install:lock+fsync recovery;materialize+backup only→replace all managed Skills;preserve user AGENTS;install-global-agents=>backup/restore;no routing/validator/platform/regression/parity/attestation/Ending/hash;write failure=>restore.GitHub push→gate before README/index/commit/remote;user-only retirement;Sites build+host only.

## Project Cache artifact policy

Cache:project support only in `<project-root>/Cache/`;one-task scratch/test/intermediate=>`tmp-*`;short reuse=>`<YYYYMMDD>`+reason/review;`remote-*`/`remote-test/`=>explicit retain only;formal reusable tests stay source;no global/top-level tmp/tests/work;paths root-relative/native;external absolute=>AI registry;Cache=>AGENTS ownership/retention/delete;details=>Obsidian.
