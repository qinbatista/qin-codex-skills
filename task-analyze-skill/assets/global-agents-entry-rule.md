Merge this section into `~/.codex/AGENTS.md` and `~/AGENTS.md`.

# Task Lifecycle

Route by task;notify user early.

Ending is evidence-driven: start one detached global projectless Ending only when a released result exposes `real_test`, `information_update`, or `memory_update`; code normally exposes `real_test` through one Quick Check. No surface records `intentionally_skipped_simple_task`;`ending_skip_reason=no_real_test_or_information_or_memory_update`;`intentionally_skipped_simple_task`=>no End Task. Skills→code→Quick Check→`CODE READY`;broad verification→Ending.No raw prompts/results/reasoning,paths,secrets,emails,IDs.

Score every submission 0-100:small 0-24,standard 25-49,complex 50-74,advanced 75-100;show compact score/model/route status.Resolve entry first;exact step-capability/band history wins:Sol/high may downgrade,Luna-max/lower may upgrade;no match stays <=entry;reuse recovered/lowest-correct.native project→Model Switch→category→shared-category wikilinks;no JSON sidecar;2+ material result stages auto-activate one graph;each result/Ending records score,band,pair,purpose,deps,stop.Eligible low-risk low-ambiguity text/code/write/execute score<=24;same-session gate→Spark-low if no stronger route;Ending quality FAIL suppresses that fingerprint.Dependency-ready independent nodes run parallel;shared writes/order/output deps stay linear.resolve CODEX_HOME;pipe exact task once via platform Python to `skills/task-analyze-skill/scripts/obsidian_adaptive_model_runner.py`;never read/edit/answer inline.

LOCKED_ROUTE_NODE loads required Skills,does code/result+one Quick Check,emits CODE READY,stops;parent launches one End only after the final aggregate `ending-required` receipt and release;child/subprocess receipts never launch it.ENDING_TASK_WORKER is the sole lifecycle controller:gpt-5.3-codex-spark|xhigh;score scopes checks only;unavailable=>registry-floor Luna-low;no nested End/Fix;same End Task continues one bounded action at a time.ENDING_CHECK_WORKER reads listed Skills,runs one check,writes evidence only.Multi-node saves schema-2 dynamic_task_graph;calls task_route_dispatcher.py run-plan;no benchmark.

Exact one-source/tool/image read-only uses `task_complexity_score.py`;no reread/full-read/precheck;output artifact MUST runner.2 Real PASS trial down 1 rung;quality FAIL up 1;missing Obsidian queues projection;local history continues.Producer owns files/skills/one Quick Check only.Only the final aggregate `ending-required` or `ending_required=true` receipt after all result nodes settle=>one independent global projectless Ending when real_test/information_update/memory_update exists;resolve root with codex_app__list_projects;ending_verification_plan.py create-launches --producer-receipt(final) --project-id(context-only);call codex_app__create_thread target;codex_app__list_threads;projectId=null/absent;ack threadId+hostId+originProjectId+threadProjectId=null+actual pair;audit-launches end_task_trigger_rate=100%;project/current-task/same-task-subtask placement;missing acknowledgement=>BLOCKED;all checks PASS.

End Task stays globally visible;never auto-archive/delete;terminal memory/classification/record closeout.Ending:FAIL/mismatch;attempts,first/retry pass,suitability,next pair/action,Obsidian link/status;immutable origin session;codex_app__send_message_to_thread;repair_prompt;fresh global projectless End Task;--repair-of-lifecycle-id;up to 3 repairs;missing source session or failed prompt submission is BLOCKED;BLOCKED only unavailable/external/limit;never emulate another Ending,repair in verifier,or wait for origin;bounded check-worker waits occur only inside detached Ending.Terminal events write local+same-ID Obsidian records;only producer receipts move routing.

Host command execution is non-interactive:use current exec_command/Python;approval_policy="never";never open Terminal.app, Computer Use, or a second terminal to click Allow;host Allow prompt is BLOCKED—stop and report;never click or emulate approval.

future routes merge/dedupe pairs,fingerprint,score/band,direction,recovery.AUTO_BENCHMARK_ENTRY:Direct `gpt-5.6-sol|ultra`;Direct Sol-ultra no Skill;Auto `gpt-5.6-luna|max`;Auto Luna-max stable child/graph;MUST child/graph then return child JSON;gate owns separate Ending;excludes only controller;compare task vs +Ending;all exact PASS;all result/Ending PASS;Primary=time+tokens;totals lower;route/calibration/Ending separate;first-result diagnostic;Sol probe exact result+route.Final Ending PASS/BLOCKED.

`model_identity_disclosure.py render`:Complexity: · Model: · Route: · Evidence: runtime receipt|verified entry (no runtime receipt)|task assignment (no runtime receipt)|configured selection (no runtime receipt);changed route:Model path:;`render --manifest` lists score/band/pair/purpose/deps/status in Model stages;receipt wins;unavailable=unknown|unknown;full routing data. Process Skill/AGENTS,real execution,effective project-result memory agree;Ending appends only memory-only correction;Skill/execution defects return origin;Next tasks read effective project-result memory only.

Unity:report project-relative path;read ProjectVersion.txt;list Editors;resolve `unity editors path <version>`;confirm binary;CLI.External Editor path from valid `Cache/cache_path.json` or discovery+register.Unavailable/unsupported/fail:report+fallback.Never change/install/substitute without approval. Host code loads `cross-platform-execution`:portable Python for Windows/macOS/Linux;no `.cmd`/`.sh` selectors.Docker/Unity keep runtime.Skill code runs `prompt-skill` platform checks.

Never control Obsidian UI.Validate `Cache/cache_path.json`;then `CODEX_OBSIDIAN_VAULT`;then `obsidian.json` open-vault;then one bounded search.Cache result;read MyAILLM `AGENTS.md`+project `Knowledge.md`.Unresolved:stop before work,report keys/search.Cold start never waives context.

Global Skill:capability catalog/full gate PASS before deploy/commit/push;missing/stale/fail blocks;only user retires.Website=>`sites:sites-building`;publish=>`sites:sites-hosting`;no substitute;research/navigation excluded.

## Project Cache artifact policy

project-support write:<project-root>/Cache/ test scripts/intermediate code ordinary task artifacts default `tmp-<folder-name>` or `<YYYYMMDD>`; `remote-<name>`: user explicitly asks to retain/save or project contract explicitly marks it as retained; `remote-test/` retained tests;ordinary test scratch; no top-level `tests/` legacy top-level directory; `~/.codex/cache`, `~/.codex/tmp`, `tmp/`, `tests/`, `work/` forbidden;local-machine path not only Cache paths; project-root-relative/runtime native path APIs; `Cache/cache_path.json` bounded AI-only project-external; important Cache owner/source explicit authorization;project-root `AGENTS.md` compact structural contract, not a project notebook; ownership boundaries/critical entry points/hard constraints;Do not write implementation details; task history/test results/generated data/troubleshooting; Obsidian
