Merge this section into `~/.codex/AGENTS.md`.

# Task Lifecycle

Score every submission 0-100 before task files:small 0-24,standard 25-49,complex 50-74,advanced 75-100;show compact score/model/route status.Resolve entry first;exact step-capability/band history wins across roots:Sol/high may downgrade,Luna-max/lower may upgrade;no match stays <=entry;reuse recovered/lowest-correct.Route memory by native project→Model Switch→category→shared-category wikilinks;read current+shared+exact-linked categories;no JSON sidecar.Split distinct work only;2+ material result stages auto-activate one graph;each result/Ending records score,band,pair,purpose,deps,stop;parent score never fixes the project model.Eligible low-risk low-ambiguity text/code/write/execute score<=24 executes Spark-low first;the entry routes it instead of answering inline;exceptions only modality,risk,ambiguity,shared-state,capability;zero-token failure gets one quality fallback;Ending quality FAIL suppresses that fingerprint.Dependency-ready independent nodes run in parallel;shared writes/order/output deps stay linear.Before eligible text/code,entry MUST resolve CODEX_HOME and pipe exact task once via platform Python at `skills/task-analyze-skill/scripts/obsidian_adaptive_model_runner.py`;never read/edit/answer inline.multi-node work saves one schema-2 `dynamic_task_graph` then calls `task_route_dispatcher.py run-plan` once.Use max_safe,one item/result,unsplit continuity,and model_switch_summary.Never require a benchmark for a valid graph.Exact one-source/tool/image uses `task_complexity_score.py`;no reread/full-read/precheck.2 Real PASS down 1 rung;quality FAIL up 1;task assignment/no_switch until receipt;missing Obsidian uses local history,queues projection,no block.Producer owns files/skills/Quick Check;heavy/API/large/side-effect checks syntax+names+references.End Task required after result:score each check;select pair/start ledger;run ending_verification_plan.py create-launches;call every codex_app__create_thread;ack each threadId+hostId;require audit-launches end_task_trigger_rate=100%;handoff alone is pending;link global projectless End/Fix Tasks;all checks PASS.PASS records then self-archives;FAIL records evidence,creates Fix then fresh End Task,up to 3 repairs;BLOCKED only unavailable/external/limit;never same-task subtask/emulate/wait/self-verify.Terminal receipts write local history first,then the same event ID to Obsidian;future routes merge/dedupe entry/selected/effective/next pairs,step fingerprint,score/band,direction,recovery.No hook.Final PASS/BLOCKED Ending-only.

`model_identity_disclosure.py render` outputs every origin/result/Ending verbatim:`Complexity:`+`· Model:`+`· Route:`,then `Evidence:`=`runtime receipt|verified entry (no runtime receipt)|task assignment (no runtime receipt)|configured selection (no runtime receipt)|unavailable`.A changed route adds `Model path:`.For 2+ model-executed result/Ending stages,`render --manifest` MUST append `Model stages (N):` with every stage score/band/pair/purpose/deps/status;never show only the last management pair.Receipt wins;else known pair;only unavailable=`unknown|unknown`;full routing data stays in receipts/ledgers.

AUTO_BENCHMARK_ENTRY:Direct `gpt-5.6-sol|ultra`;Direct Sol-ultra no Skill;Auto `gpt-5.6-luna|max`;Auto Luna-max stable child/graph;MUST child/graph then return child JSON;gate owns separate Ending;excludes only controller;compare task vs +Ending;all exact PASS;all result/Ending PASS;Primary=execution time+tokens;primary totals lower;route/calibration/Ending separate;first-result diagnostic;Sol probe exact result+route;

Unity:report the project path relative to its root;read ProjectVersion.txt;list Editors;resolve `unity editors path <version>`;confirm binary;use CLI first.If the external Editor binary needs an absolute path,use a validated `Cache/cache_path.json` entry or discover it once and register it there.On unavailable/unsupported/one failed diagnosis,report then fallback.Never change/install/substitute without approval.

Never control Obsidian UI.First validate the project's `Cache/cache_path.json` Obsidian entries;then try readable `CODEX_OBSIDIAN_VAULT`,then `obsidian.json` open-vault,then one exact bounded search.Cache each verified result in that registry;read MyAILLM `AGENTS.md`+project `Knowledge.md` by connector/filesystem.If unresolved,stop before work and report relative registry keys/search status.Cold start never waives context.

Host code loads `cross-platform-execution`:portable Python for Windows/macOS/Linux;no `.cmd`/`.sh` selectors.Docker/Unity keep runtime.Skill code runs `prompt-skill` platform checks.

## Project Cache artifact policy

Before any project-support write,resolve `<project-root>`;put test scripts/results and intermediate code,logs/debug,receipts/probes in `<project-root>/Cache/` using `Cache/tests/<task>`,`Cache/debug/<task>`,`Cache/images/<task>`.Never use `~/.codex/cache`,`~/.codex/tmp`,or project-root `tmp/`,`tests/`,`work/`;OS/tool temp is excluded.

All local-machine paths—not only Cache paths—in skills/scripts/code/config/docs/commands must be project-root-relative or runtime-derived;never hard-code POSIX home absolute path/Windows drive-letter absolute path;run from project root with native path APIs.

Only unavoidable machine-specific absolute paths for AI access to project-external resources may be in project-root `Cache/cache_path.json`.It is untracked AI-only state;validate with one bounded platform-aware discovery and explicit authorization;replace the registry atomically,preserve unrelated keys,and never store credentials or publish.

project-root `AGENTS.md` is a compact structural contract,not a project notebook.Keep stable structure,ownership boundaries,critical entry points,hard constraints,and one concise registry entry with path+role+owner/source+retention/version-control status.Do not write implementation details,task history,test results,generated data,or troubleshooting;put details in owning source, project documentation, or a README;document important Cache only with explicit authorization.Update `AGENTS.md` only when structure/ownership/entrypoint/constraint changes.

Important Cache:`Cache/management-skill-sync/`—untracked disposable mirror scratch owned by `sync_global_skills.py`;details stay with the script.
Important Cache:`Cache/cache_path.json`—untracked AI-only external absolute-path registry;project code must never read it.
