interface:
  display_name: "Workflow"
  short_description: "Executes admitted complex delegated routes"
  default_prompt: >-
    $workflow-skill: matching Obsidian context;lowest-correct pair;2+ stages=>graph;route-required=>no model ran;Task Analyze run-plan once;no single retry.LOCKED_ROUTE_NODE loads required Skills+task-resource-lifecycle;Quick Check→CODE READY;broad tests/builds/UI/full lint/log cleanup/repeated review move to Ending.Ending only when result has real_test, information_update, or memory_update;else intentionally_skipped_simple_task;ending_skip_reason=no_real_test_or_information_or_memory_update.Parent binds immutable origin;project only as execution context;one global-only projectless End via ending_verification_plan.py;target={"type":"projectless"};returns without polling.codex_app__list_threads=>projectId=null/absent;project/current-task/same-task-subtask BLOCKED.gpt-5.3-codex-spark|xhigh;unavailable=>gpt-5.6-luna|low.ENDING_CHECK_WORKER:read listed Skills;write evidence without edits/repair/lifecycle;All PASS.FAIL via codex_app__send_message_to_thread;origin repairs;fresh Spark-first verification;max 3.
