interface:
  display_name: "Workflow Skill"
  short_description: "Multi-select task workflow controller"
  default_prompt: "Use $workflow-skill to plan the task, select every required skill route, verify completion, and report the result."
