interface:
  display_name: "Workflow Skill"
  short_description: "Lightweight routing or full task workflow"
  default_prompt: "Use $workflow-skill to choose lightweight routing for simple direct work, or the full workflow for concrete coding, file-changing, multi-step, or evidence-heavy tasks."
