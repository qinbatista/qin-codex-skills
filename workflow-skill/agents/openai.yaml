interface:
  display_name: "Workflow Skill"
  short_description: "Always-first task workflow controller"
  default_prompt: "Use $workflow-skill first to plan the task, select every required executor skill route, verify completion, and report the result."
