interface:
  display_name: "Workflow Skill"
  short_description: "Start with a workflow diagram, then route the task"
  default_prompt: "Use $workflow-skill to show the required start diagram, run the Prompt Task Gate before generic text/code routing, show Models by phase before task action, choose lightweight direct routing or the full task workflow, route Python/C# coding, prompt files/templates/strings, system/developer/user instructions, AI output behavior, prompt trigger fixes, any prompt-related task, file-changing, visual/image, multi-step, or evidence-heavy work through the right executor skills, force ordinary text writing, command-line/simple checks, Python/C# code writing, and code tests/probes to GPT-5.3-Codex-Spark unless image reading, comprehensive reading/checking, deep review, or final judgment is required, use the internal ChatGPT-in-Chrome image route when useful, verify with $verify-skill, and run optimization only when the explicit/repeated/reusable gate passes."
