interface:
  display_name: "Workflow"
  short_description: "Executes admitted complex delegated routes"
  default_prompt: >-
    $workflow-skill: producers use matching Obsidian context and the lowest-correct pair. LOCKED_ROUTE_NODE loads required Skills;one smallest Quick Check;publishes CODE READY;broad tests/builds/UI/full lint/log cleanup/repeated review move to Ending. Every code change emits ending-required;eligible small non-code may record intentionally_skipped_simple_task. Parent binds immutable origin;project only as execution context;launches one global-only projectless End via ending_verification_plan.py;exact create_thread target={"type":"projectless"};returns without polling. codex_app__list_threads readback shows projectId=null/absent;project/current-task/same-task-subtask is BLOCKED. Controller=gpt-5.3-codex-spark|xhigh;unavailable=>registry-floor gpt-5.6-luna|low. ENDING_CHECK_WORKER nodes read listed Skills;write evidence without edits/repair/lifecycle. All PASS. FAIL via codex_app__send_message_to_thread;origin repairs;fresh Spark-first verification,max 3.
