# Adaptive And Admitted Routing Matrix

Eligible ordinary text/code production enters the compact adaptive runner exactly once, executes the saved contextual Luna/Terra/Sol quality pair, and does not load the full `task-analyze-skill` unless routing strategy, benchmarking, maintenance, or a graph is in scope. Tool-only, image/mixed, and exact one-source read-only work stay inline. Two or three independent read-only sources first cost-admit one contextual producer versus a graph. `workflow-skill` executes only a positively admitted multi-node or locked route. Verification belongs to a separate post-result Ending task.

| Scenario | Use when | Foreground route | Model pattern | First presentation | Ending Task |
|---|---|---|---|---|---|
| ordinary-production | One eligible text/code result, edit, or implementation. | entry bootstrap -> adaptive producer -> result | Saved contextual quality pair; one stronger fallback only after zero result. | For code, run one bounded Quick Check; then present. | Create `End Task-{related task}` and return without waiting. |
| exact-one-source-read | One exact named-source read-only result. | one bounded inline read -> result | Current entry model; no producer receipt or graph. | Present immediately after the single bounded read. | Create the separate Ending task only when task tools are callable. |
| tool-only-action | Open, navigate, send a bounded tool command, or inspect current tool state. | inline tool -> result | No model-generated producer route. | Report the observable result immediately. | Evidence-only state confirmation; never repeat the action. |
| image-or-mixed | The work requires image generation/editing or a mixed visual tool surface. | inline owning image/tool skill -> result | Use the owning tool contract; no fabricated producer receipt. | Present the generated or edited artifact. | Read-only artifact/evidence audit. |
| prompt-production | Create or change a reusable prompt or durable AI instruction. | adaptive producer -> prompt-skill -> result | Contextual quality pair selected by task strategy. | Present the completed prompt first. | Representative trials and deterministic evidence afterward. |
| ordinary-code-domain | Implement or edit an active registry-owned code domain. | adaptive producer -> code-skill -> Quick Check -> result | Contextual quality pair; receipt proves the actual execution model. | Present changed paths, behavior, and Quick Check. | Evidence-only audit in a new persistent task. |
| independent-small-sources | Two or three explicit read-only sources fit the cost budget. | entry bootstrap -> one adaptive producer -> result | Pre-read estimate chooses one session to avoid repeated context. | Present the merged exact result. | Validate receipts, result, and source boundary. |
| independent-large-sources | Context pressure exceeds the source budget or parallel latency is explicitly required. | entry bootstrap -> admitted source graph -> adaptive merge -> result | Spark-low source branches plus the contextual merge pair; every stage receipted. | Present the merged exact result. | Validate the aggregate receipt and immutable evidence. |
| dependent-multi-file | Edits or evidence depend on shared state across files. | one adaptive producer -> owning skill -> result | Never split dependency-coupled work merely because it has many files. | Present completed result after proportional Quick Check. | Separate evidence-only Ending task. |
| explicit-routing-no-graph | User asks for routing but no multi-node graph passes admission. | Task Analyze -> one contextual producer -> result | Quality selection still applies; no open-ended graph. | Present result immediately. | Real verdict and learning after presentation. |
| task-analyze-maintenance | Maintain routing contracts, runners, or benchmark assets. | adaptive producer -> Task Analyze -> owning code/docs skills -> result | One dependency-coupled producer owns files, tests, report, and handoff. | Present completed change and focused test evidence. | New persistent Ending task; origin never waits. |
| explicit-benchmark | User authorizes Direct versus Auto calibration. | Task Analyze -> fixed Direct and receipt-proven Auto arms -> report | Both entries use the declared pair; Direct has no verifier; Auto task and Ending are separated. | First-result timestamp stops at exact result availability. | Ending cost is later and explicit; controller is a disclosed diagnostic. |
| admitted-complex | Current evidence admits a real dependency graph. | Task Analyze -> Workflow -> dependency-ready locked nodes -> result | Exact locked pairs; one execution surface per branch. | Release merged completed result. | Real Verify after release; no foreground gate. |

## Goal Rules

- Every eligible ordinary text/code production task enters the adaptive runner once; exact one-source, tool-only, and image/mixed work remain inline.
- One receipt-valid Real PASS retains the selected pair, two matched PASS outcomes try one lower rung, and quality failure upgrades one rung.
- Spark is schedule-source-only and never overrides the contextual quality pair.
- Fan-out requires pre-read cost admission; dependent multi-file work always stays with one producer.
- Full Task Analyze activates only for explicit routing/benchmark/maintenance or graph evaluation; activation alone does not admit an open-ended graph.
- Every active registry-owned code-domain implementation uses `code-skill`.
- Main Result and proportional Quick Check precede all Ending verification.
- The origin creates and links a persistent `End Task-{related task}`, then returns without waiting, polling, repairing, or using a same-task subtask.
- Runtime receipts prove observed routing; labels and availability probes do not.
- Direct uses the exact raw prompt through `--direct-task`; Auto uses it through `--bootstrap-task`.
- The public two-world comparison is Direct task with zero verifier versus Auto task plus separate Ending. The common Sol-ultra dispatcher/controller is disclosed but excluded from those task/check worlds.
- Savings require complete like-for-like evidence per task class. Logical tokens are not billing tokens.
- Do not publish or push unless explicitly requested.
