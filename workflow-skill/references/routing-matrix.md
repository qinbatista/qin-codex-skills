# Inline And Admitted Routing Matrix

Ordinary requests stay on the current model regardless of apparent complexity. They present the completed result immediately and never load full `task-analyze-skill` or `workflow-skill`. Full Task Analyze loads only for explicit routing/benchmark/maintenance or a real graph under consideration; activation still returns to inline unless comparable end-to-end evidence positively admits delegation. Workflow executes admitted routes only. Verification belongs to post-result Ending Real.

| Scenario | Use when | Foreground route | Model pattern | First presentation | Ending Task |
|---|---|---|---|---|---|
| ordinary-direct | One answer, read, command, open, or edit. | inline-current-model -> result | Current user-selected model; no foreground child, receipt, or dispatcher. | For code, one bounded Quick Check; then present. | Create and return from `End Task-{related task}` for proportional Real Verify. |
| open-chrome | Open the user's Chrome profile. | inline-current-model -> chrome:control-chrome -> result | Tool-only inline action. | Report completion immediately. | Confirm observable Chrome state. |
| open-youtube | Open Chrome and YouTube. | inline-current-model -> chrome:control-chrome -> result | Tool-only inline action. | Report completion immediately. | Confirm `youtube.com`. |
| search-cctv-on-youtube | Open Chrome, YouTube, and search CCTV. | inline-current-model -> chrome:control-chrome -> result | Tool-only inline action. | Report completion immediately. | Confirm query/results. |
| design-youtube-like-website | Build a YouTube-like frontend. | inline-current-model -> frontend skill -> result | Current model performs the build; apparent complexity alone never admits delegation. | Present the completed draft. | Render and interaction Real Verify. |
| ordinary-code-domain | Implement or edit an active code domain. | inline-current-model -> code-skill -> result | Current model follows domain rules; no foreground trial or verifier. | Present changed path and behavior. | Syntax/compile/focused execution and relevant regression. |
| explicit-routing-no-admission | User asks for routing but comparable admission is absent. | task-analyze-skill -> inline-current-model -> result | Current model completes the task. | Present result immediately. | Real Verify after presentation; no Workflow child. |
| task-analyze-maintenance-no-admission | Maintain routing contracts without positive delegation evidence. | task-analyze-skill -> inline-current-model -> code-skill -> result | Full skill may be read for maintenance; no delegated foreground. | Present completed change. | Contract tests and live replay afterward. |
| explicit-benchmark | User authorizes Direct versus Global calibration. | task-analyze-skill -> benchmark runner -> result timestamps -> external Real gate | Direct uses raw `--direct-task`; Global uses raw `--bootstrap-task`; both stay outside entry context. | `first_result_elapsed_ms` stops at result availability. | Exact correctness, receipts, census, and reports are excluded from first-result latency. |
| admitted-single | Current evidence admits one delegated node. | task-analyze-skill -> workflow-skill -> producer -> result | Frozen receipt-backed Real-passing `trial=false` pair. | Release completed producer output. | Real Verify and learning after release. |
| admitted-complex | Current evidence admits a real dependency graph. | task-analyze-skill -> workflow-skill -> dependency-ready result nodes -> result | Exact locked pairs; one execution surface per branch. | Release merged completed result. | Real Verify, optimization proof, docs, or memory afterward. |

## Goal Rules

- The current user-selected model performs ordinary inline work; it is not route-only.
- Full Task Analyze activates only for explicit routing/benchmark/maintenance or graph evaluation; activation alone does not admit Workflow.
- Every active registry-owned code-domain implementation uses `code-skill`.
- Main Result precedes all verification.
- Ending Real Verify, optimization proof, reports, logs, docs, and memory begin in the separate End Task thread after first presentation.
- A background correctness failure notifies the user, reopens the task, repairs, and presents the correction.
- Runtime receipts prove observed delegated routing; labels and availability probes do not.
- Direct uses the exact raw prompt through `--direct-task`; Global uses it through `--bootstrap-task`.
- Savings require complete like-for-like evidence per task class. Token cost includes all foreground sessions; user-visible time includes required Quick Check and ends at first presentation, excluding the detached Ending thread.
- Do not publish or push unless explicitly requested.
