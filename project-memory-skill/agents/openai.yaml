interface:
  display_name: "project-memory-skill"
  short_description: "Scoped recall and concise memory-only closeout"
  default_prompt: "Ensure an Obsidian memory vault, then read exact-project memory. Summarize durable outcomes and preferences with the selected model and effort. Skip absent matching notes; preserve project isolation."
